services:
  pg:
    image: postgres
    restart: unless-stopped
    ports:
      - 5432:5432
    volumes:
      - pg-test:/var/lib/data
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_DB: ${POSTGRES_DB}


volumes:
  pg-test:
    name: pg-test
      